# Практическое руководство: Настройка SNMP на домашних устройствах

## 🏠 Быстрый старт для тестирования

### Вариант 1: Тестирование на вашем компьютере (Windows)

#### Шаг 1: Включите SNMP на Windows

1. **Откройте PowerShell от имени администратора**

2. **Включите SNMP службу:**
   ```powershell
   # Проверьте, установлен ли SNMP
   Get-WindowsOptionalFeature -Online -FeatureName SNMP
   
   # Если не установлен, установите:
   Enable-WindowsOptionalFeature -Online -FeatureName SNMP
   ```

3. **Настройте SNMP через реестр:**
   ```powershell
   # Откройте regedit
   regedit
   ```
   
   Перейдите к: `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\SNMP\Parameters\`
   
   Создайте или измените:
   - `ValidCommunities` (REG_MULTI_SZ) - добавьте значение `1` (это "public")
   - `PermittedManagers` (REG_MULTI_SZ) - оставьте пустым для разрешения всех

4. **Или используйте GUI:**
   - Откройте `services.msc`
   - Найдите "SNMP Service"
   - Правый клик → Properties → Security
   - Добавьте community "public" с правами "READ ONLY"
   - Нажмите OK и перезапустите службу

5. **Проверьте, что SNMP работает:**
   ```powershell
   # Узнайте IP адрес вашего компьютера
   ipconfig
   # Запомните IPv4 адрес (например, 192.168.1.100)
   ```

#### Шаг 2: Добавьте устройство в систему

1. **Узнайте IP адрес вашего компьютера:**
   ```powershell
   ipconfig
   # Запишите IPv4 адрес (например, 192.168.1.100)
   ```

2. **Добавьте устройство через API:**
   ```bash
   curl -X POST "http://localhost:8000/add_device" \
        -H "Content-Type: application/json" \
        -d '{
          "name": "Мой компьютер",
          "category": "Computer",
          "place_id": "Дом",
          "manufacturer": "Windows PC"
        }'
   ```
   Запомните `id` устройства из ответа.

3. **Настройте SNMP конфигурацию:**
   ```bash
   curl -X POST "http://localhost:8000/snmp/config" \
        -H "Content-Type: application/json" \
        -d '{
          "device_id": 1,
          "enabled": true,
          "ip_address": "192.168.1.100",
          "port": 161,
          "community": "public",
          "version": "2c"
        }'
   ```
   Замените `device_id` и `ip_address` на ваши значения.

4. **Проверьте SNMP:**
   ```bash
   curl "http://localhost:8000/snmp/check/1"
   ```

---

### Вариант 2: Тестирование на роутере

#### Шаг 1: Настройте SNMP на роутере

**Для большинства роутеров (TP-Link, ASUS, D-Link):**

1. Войдите в веб-интерфейс роутера (обычно `192.168.1.1` или `192.168.0.1`)
2. Найдите раздел:
   - **"SNMP"** или
   - **"Network Management"** или
   - **"Advanced Settings" → "SNMP"**
3. Включите SNMP
4. Установите:
   - **Community String:** `public`
   - **Version:** `2c` (или `v2c`)
   - **Port:** `161`
5. Сохраните настройки

**Для роутеров с OpenWRT:**
```bash
# Подключитесь по SSH к роутеру
opkg update
opkg install snmpd
# Настройте /etc/config/snmpd
/etc/init.d/snmpd enable
/etc/init.d/snmpd start
```

#### Шаг 2: Добавьте роутер в систему

```bash
# 1. Добавьте устройство
curl -X POST "http://localhost:8000/add_device" \
     -H "Content-Type: application/json" \
     -d '{
       "name": "Домашний роутер",
       "category": "Router",
       "place_id": "Дом",
       "manufacturer": "TP-Link"
     }'

# 2. Настройте SNMP (замените IP на IP вашего роутера)
curl -X POST "http://localhost:8000/snmp/config" \
     -H "Content-Type: application/json" \
     -d '{
       "device_id": 2,
       "enabled": true,
       "ip_address": "192.168.1.1",
       "port": 161,
       "community": "public",
       "version": "2c"
     }'

# 3. Проверьте
curl "http://localhost:8000/snmp/check/2"
```

---

### Вариант 3: Тестирование на принтере

#### Шаг 1: Настройте SNMP на принтере

1. Откройте веб-интерфейс принтера (обычно через IP адрес)
2. Найдите раздел **"Network"** → **"SNMP"**
3. Включите SNMP
4. Установите:
   - **Community:** `public`
   - **Version:** `2c`
5. Сохраните настройки

#### Шаг 2: Добавьте принтер в систему

```bash
# 1. Добавьте устройство
curl -X POST "http://localhost:8000/add_device" \
     -H "Content-Type: application/json" \
     -d '{
       "name": "Домашний принтер",
       "category": "Printer",
       "place_id": "Дом",
       "manufacturer": "HP"
     }'

# 2. Настройте SNMP (замените IP на IP вашего принтера)
curl -X POST "http://localhost:8000/snmp/config" \
     -H "Content-Type: application/json" \
     -d '{
       "device_id": 3,
       "enabled": true,
       "ip_address": "192.168.1.50",
       "port": 161,
       "community": "public",
       "version": "2c"
     }'

# 3. Проверьте
curl "http://localhost:8000/snmp/check/3"
```

---

### Вариант 4: Тестирование на Linux (Ubuntu/Debian)

#### Шаг 1: Установите и настройте SNMP

```bash
# Установите SNMP агент
sudo apt update
sudo apt install snmpd snmp

# Настройте SNMP
sudo nano /etc/snmp/snmpd.conf
```

Добавьте или измените строки:
```
# Разрешаем доступ с любого IP (для тестирования)
rocommunity public 0.0.0.0/0

# Или только с вашего IP
rocommunity public 192.168.1.0/24

# Системная информация
syslocation "Home"
syscontact "Your Email"
```

```bash
# Перезапустите службу
sudo systemctl restart snmpd
sudo systemctl enable snmpd

# Проверьте статус
sudo systemctl status snmpd

# Узнайте IP адрес
hostname -I
```

#### Шаг 2: Добавьте в систему

```bash
# 1. Добавьте устройство
curl -X POST "http://localhost:8000/add_device" \
     -H "Content-Type: application/json" \
     -d '{
       "name": "Linux сервер",
       "category": "Server",
       "place_id": "Дом",
       "manufacturer": "Ubuntu"
     }'

# 2. Настройте SNMP (замените IP)
curl -X POST "http://localhost:8000/snmp/config" \
     -H "Content-Type: application/json" \
     -d '{
       "device_id": 4,
       "enabled": true,
       "ip_address": "192.168.1.200",
       "port": 161,
       "community": "public",
       "version": "2c"
     }'

# 3. Проверьте
curl "http://localhost:8000/snmp/check/4"
```

---

## 🔍 Проверка SNMP перед добавлением в систему

### Windows (PowerShell):

```powershell
# Установите net-snmp (если нужно)
# Или используйте встроенные инструменты

# Проверьте SNMP на локальной машине
snmpwalk -v2c -c public localhost 1.3.6.1.2.1.1.1.0
```

### Linux:

```bash
# Проверьте SNMP на локальной машине
snmpwalk -v2c -c public localhost 1.3.6.1.2.1.1.1.0

# Или с другого компьютера (замените IP)
snmpwalk -v2c -c public 192.168.1.100 1.3.6.1.2.1.1.1.0
```

### Онлайн инструменты:

- **SNMP Tester** - https://www.snmptester.com/
- **SNMP Check** - различные онлайн сервисы

---

## 🛠️ Быстрый тест через Python

Используйте скрипт `test_snmp_connection.py` для проверки подключения:

```bash
python test_snmp_connection.py 192.168.1.1 public
```

---

## 📋 Типичные проблемы и решения

### Проблема 1: "No SNMP response"

**Причины:**
- SNMP не включен на устройстве
- Неправильный IP адрес
- Firewall блокирует порт 161
- Неправильный community string

**Решения:**
1. Проверьте, что SNMP включен на устройстве
2. Проверьте IP адрес: `ping 192.168.1.1`
3. Проверьте firewall:
   ```powershell
   # Windows - разрешите порт 161
   netsh advfirewall firewall add rule name="SNMP" dir=in action=allow protocol=UDP localport=161
   ```
4. Попробуйте другой community string

### Проблема 2: "Timeout"

**Причины:**
- Устройство перегружено
- Медленная сеть
- Неправильные настройки таймаута

**Решения:**
1. Увеличьте таймаут в настройках
2. Проверьте сетевое соединение
3. Попробуйте позже

### Проблема 3: "Access denied"

**Причины:**
- Неправильный community string
- Ограничения доступа на устройстве

**Решения:**
1. Проверьте community string в настройках устройства
2. Убедитесь, что ваш IP разрешен для SNMP доступа

---

## ✅ Чек-лист для успешного подключения

- [ ] SNMP включен на устройстве
- [ ] Community string установлен (обычно "public")
- [ ] Версия SNMP установлена (обычно "2c")
- [ ] Порт 161 открыт в firewall
- [ ] IP адрес устройства правильный
- [ ] Устройство добавлено в систему через API
- [ ] SNMP конфигурация создана через API
- [ ] Тест SNMP прошел успешно

---

## 🎯 Следующие шаги

После успешного подключения:

1. **Настройте автоматический мониторинг:**
   ```bash
   python snmp_monitor.py
   ```

2. **Проверьте все устройства:**
   ```bash
   curl "http://localhost:8000/snmp/check-all"
   ```

3. **Получите статистику:**
   ```bash
   curl "http://localhost:8000/snmp/status"
   ```

Готово! Теперь ваша система может мониторить домашние устройства через SNMP! 🎉




